<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="origin_list_view_list" model="ir.ui.view">
        <field name="name">origin.list.view.list</field>
        <field name="model">origin.list</field>
        <field name="arch" type="xml">
            <list string="Origin" editable="bottom">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
            </list>
        </field>
    </record>

    <record id="origin_list_view_search" model="ir.ui.view">
        <field name="name">origin.list.view.search</field>
        <field name="model">origin.list</field>
        <field name="arch" type="xml">
            <search string="Origin">
                <field name="name"/>
                <separator/>
                <filter string="Archived" name="inactive" domain="[('active', '=', False)]"/>
            </search>
        </field>
    </record>

    <record id="origin_list_action" model="ir.actions.act_window">
        <field name="name">Origins</field>
        <field name="res_model">origin.list</field>
        <field name="view_mode">list</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                No Origin found. Let's create one!
            </p>
        </field>
    </record>
</odoo>
