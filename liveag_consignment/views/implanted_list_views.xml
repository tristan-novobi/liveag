<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="implanted_list_view_list" model="ir.ui.view">
        <field name="name">implanted.list.view.list</field>
        <field name="model">implanted.list</field>
        <field name="arch" type="xml">
            <list string="Implanted List" editable="bottom">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
            </list>
        </field>
    </record>

    <record id="implanted_list_view_search" model="ir.ui.view">
        <field name="name">implanted.list.view.search</field>
        <field name="model">implanted.list</field>
        <field name="arch" type="xml">
            <search string="Implanted List">
                <field name="name"/>
                <separator/>
                <filter string="Archived" name="inactive" domain="[('active', '=', False)]"/>
            </search>
        </field>
    </record>

    <record id="implanted_list_action" model="ir.actions.act_window">
        <field name="name">Implanted List</field>
        <field name="res_model">implanted.list</field>
        <field name="view_mode">list</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                No record found. Let's create one!
            </p>
        </field>
    </record>
</odoo>
